name: qd-vip
on:
  workflow_dispatch:

jobs:
  t:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: 装依赖
        run: |
          set +e
          pip3 install --quiet requests 2>&1 | tail -1
          exit 0

      - name: CI IP 下测 getvipcontent
        env:
          QD_SESS: ${{ secrets.QD_SESS_900069660351 }}
        run: |
          set +e
          mkdir -p /tmp/ci
          echo "$QD_SESS" > /tmp/ci/sess.json
          cat > /tmp/ci/t.py <<'PYEOF'
import json, os, sys, time, urllib.request, urllib.parse, gzip
sys.path.insert(0, os.environ.get('GITHUB_WORKSPACE','.') + '/ci')
os.environ['SESS'] = '/tmp/ci/sess.json'
os.environ['DEV_ANDROID_ID'] = os.environ.get('Q16', '7489ce4eb6aa591652700cf910001671a911')
import argus_fetch as A
print('账号:', A.YW)
def api(path, params):
    url = A.HOST + path + '?' + urllib.parse.urlencode(params)
    h = {'User-Agent': 'Mozilla/mobile QDReaderAndroid/%s/%s/%s' % (A.APPVER, A.VC, A.ASRC),
         'Accept-Encoding': 'gzip', 'Cookie': A.COOKIE, 'QDInfo': A.qdinfo(A.YW),
         'tstamp': str(int(time.time()*1000)),
         'QDSign': A.qdsign(A.canon(params), A.YW),
         'Referer': 'http://android.qidian.com'}
    try:
        r = urllib.request.urlopen(urllib.request.Request(url, headers=h), timeout=30)
        raw = r.read()
        if r.headers.get('Content-Encoding') == 'gzip': raw = gzip.decompress(raw)
        try: return json.loads(raw)
        except Exception: return {'_bin': len(raw), '_head': raw[:32].hex()}
    except Exception as e:
        return {'error': str(e)}
print('=== 登录态判据 ===')
j = api('/argus/api/v2/subscription/getunboughtchapterlist', {'bookId': '1049898328'})
print(' ', json.dumps(j, ensure_ascii=False)[:160])
time.sleep(3)
print('=== getvipcontent（VIP 章）===')
for book, chap, lbl in [('1049462635','923399275','《大汉棋圣》VIP章'),
                        ('1049898328','929343527','《请勿高考时渡劫》VIP章')]:
    j = api('/argus/api/v2/bookcontent/getvipcontent', {'bookId': book, 'chapterId': chap})
    if '_bin' in j: print('  [%s] ★ 二进制 %dB' % (lbl, j['_bin']))
    else:
        d = j.get('Data')
        c = (d.get('Content') or '') if isinstance(d, dict) else ''
        print('  [%s] Result=%s %s' % (lbl, j.get('Result'), ('★ %d 字符: %s' % (len(c), c[:90])) if c else (j.get('Message') or j.get('error',''))[:40]))
    time.sleep(8)
print('=== safegetcontent（App 主路径）===')
j = api('/argus/api/v2/bookcontent/safegetcontent', {'bookId':'1049462635','chapterId':'923399275'})
if '_bin' in j: print('  ★ 二进制容器 %dB head=%s' % (j['_bin'], j['_head']))
else: print('  Result=%s %s' % (j.get('Result'), (j.get('Message') or j.get('error',''))[:50]))
PYEOF
          python3 /tmp/ci/t.py
          exit 0
